{
    "document_class": {
        "class_name": "ingestion_manifest",
        "class_version": "2.0.0",
        "superclasses": [
            {
                "class_name": "base"
            }
        ],
        "maturity_level": "stable"
    },
    "depends_on": [
        {
            "name": "epoch_file_pattern_id",
            "mustBeNonEmpty": true,
            "documentation": "The epoch file pattern whose match produced this manifest (v1 `filenavigator_id`, id preserved). REQUIRED: it is the one edge NDI writes, restored after V_eta had dropped it for an invented `epochid`.",
            "must_refer_to_document_class": "epoch_file_pattern"
        },
        {
            "name": "epoch_id",
            "mustBeNonEmpty": true,
            "documentation": "The epoch these files were ingested for. Replaces the invented `epochid` edge, which was empty on all 6,921 documents.",
            "must_refer_to_document_class": "epoch"
        }
    ],
    "file": [],
    "fields": [
        {
            "name": "files",
            "type": "string",
            "blank_value": "",
            "default_value": "",
            "mustBeNonEmpty": false,
            "mustBeScalar": false,
            "mustNotHaveNaN": false,
            "queryable": false,
            "ontology": null,
            "documentation": "List of file references ingested for this epoch. Entries are either NDI-scheme URIs (e.g., 'epochid://t00001') or absolute filesystem paths to the source files. Length varies per epoch (3-6 entries seen in the discovery corpora).",
            "constraints": {}
        }
    ]
}
