export default function App() {
  return (
    <main>
      <h1>DID Schema Viewer</h1>
      <p>
        Browser-based viewer and editor for DID/NDI document schemas. Scaffolding
        only -- viewer and editor land in subsequent steps (see issue #28).
      </p>
    </main>
  );
}
